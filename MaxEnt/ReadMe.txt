The MaxEnt needs to be done in 2 parts:
1. Matlab - Run maxent_fwdmod.m (or maxent_fwdmod_nosig.m). 
			Save g as g.mat and v as v.mat in this folder. 
			Record gamma.
2. Python - Run mymaxent2.py code with correct gamma value. 

Save g.mat and v.mat in the proper folder after once running python code using that data. 